<?xml version="1.0"?>
<model>
  <name>first_2015_trash_can</name>
  <version>1.0</version>
  <sdf version="1.6">model.sdf</sdf>
  <description>
    Local collision-and-visual fallback for the warehouse world.  It is kept
    in the LAEA resource tree so the simulation does not depend on Gazebo's
    online model database.
  </description>
</model>
